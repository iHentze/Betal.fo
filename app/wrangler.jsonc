{
  "name": "betal-app",
  "compatibility_date": "2026-08-27",
  "compatibility_flags": ["nodejs_compat"],
  "observability": { "enabled": true },
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "betal",
      "database_id": "00000000-0000-0000-0000-000000000000",
      "migrations_dir": "migrations"
    }
  ],
  "kv_namespaces": [
    {
      // Merchant access tokens. Never leaves the worker.
      "binding": "TOKENS",
      "id": "00000000000000000000000000000000"
    }
  ],
  // Producer only. The consumer lives in workers/ingest so webhook projection is not
  // affected by app deploys or app traffic.
  "queues": {
    "producers": [{ "binding": "INGEST_QUEUE", "queue": "betal-ingest" }]
  },
  // Private company facts from api.eyga.fo. The API still requires the matching
  // EYGA_API_TOKEN Worker secret. Local development uses EYGA_API_BASE_URL.
  "services": [
    {
      "binding": "EYGA_API",
      "service": "eyga-api"
    }
  ],
  "vars": {
    "APP_URL": "https://app.betal.fo",
    "BANK_EMAIL_FROM": "Betal <banki@betal.fo>"
  }
}
