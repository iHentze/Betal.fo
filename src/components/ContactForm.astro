---
import { fo, products } from "../content/fo";
import { site } from "../content/site";
---

<form class="grid gap-6" data-contact-form>
  <div class="grid gap-5 sm:grid-cols-2">
    <label class="grid gap-2 text-sm font-medium text-white">
      {fo.contact.name}
      <input
        name="name"
        type="text"
        placeholder="Títt navn"
        autocomplete="name"
        required
        class="rounded-2xl border border-white/15 bg-white/[0.04] px-4 py-3.5 text-base font-normal text-white outline-none ring-green/50 placeholder:text-white/30 focus:border-green focus:ring-2 transition"
      />
    </label>
    <label class="grid gap-2 text-sm font-medium text-white">
      {fo.contact.company}
      <input
        name="company"
        type="text"
        placeholder="Tín fyritøka"
        autocomplete="organization"
        class="rounded-2xl border border-white/15 bg-white/[0.04] px-4 py-3.5 text-base font-normal text-white outline-none ring-green/50 placeholder:text-white/30 focus:border-green focus:ring-2 transition"
      />
    </label>
    <label class="grid gap-2 text-sm font-medium text-white">
      {fo.contact.phone}
      <input
        name="phone"
        type="tel"
        placeholder="+298 ..."
        autocomplete="tel"
        class="rounded-2xl border border-white/15 bg-white/[0.04] px-4 py-3.5 text-base font-normal text-white outline-none ring-green/50 placeholder:text-white/30 focus:border-green focus:ring-2 transition"
      />
    </label>
    <label class="grid gap-2 text-sm font-medium text-white">
      {fo.contact.email}
      <input
        name="email"
        type="email"
        placeholder="navn@fyritoka.fo"
        autocomplete="email"
        required
        class="rounded-2xl border border-white/15 bg-white/[0.04] px-4 py-3.5 text-base font-normal text-white outline-none ring-green/50 placeholder:text-white/30 focus:border-green focus:ring-2 transition"
      />
    </label>
  </div>

  <label class="grid gap-2 text-sm font-medium text-white">
    {fo.contact.product}
    <div class="relative">
      <select
        name="product"
        class="w-full appearance-none rounded-2xl border border-white/15 bg-[#0D0033] px-4 py-3.5 text-base font-normal text-white outline-none ring-green/50 focus:border-green focus:ring-2 transition cursor-pointer"
      >
        <option value="">{fo.contact.productAny}</option>
        {
          products.map((product) => (
            <option value={product.eyebrow} data-id={product.id}>
              {product.eyebrow}
            </option>
          ))
        }
      </select>
      <div class="pointer-events-none absolute right-4 top-1/2 -translate-y-1/2 text-white/50">
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
        </svg>
      </div>
    </div>
  </label>

  <label class="grid gap-2 text-sm font-medium text-white">
    {fo.contact.message}
    <textarea
      name="message"
      rows="5"
      placeholder="Greið stutt frá tínum virksemi og tørvi..."
      required
      class="resize-y rounded-2xl border border-white/15 bg-white/[0.04] px-4 py-3.5 text-base font-normal text-white outline-none ring-green/50 placeholder:text-white/30 focus:border-green focus:ring-2 transition"
    ></textarea>
  </label>

  <p data-form-error class="hidden text-sm text-red-300 bg-red-900/30 border border-red-500/30 rounded-xl p-3" role="alert"></p>

  <div class="flex flex-col gap-4 sm:flex-row sm:items-center">
    <button
      type="submit"
      class="rounded-full bg-green px-8 py-3.5 text-sm font-semibold text-navy transition-all duration-200 hover:brightness-110 hover:shadow-[0_0_20px_rgba(122,217,102,0.4)] active:scale-[0.99] cursor-pointer"
    >
      {fo.contact.submit}
    </button>
    <p class="text-xs text-mist">{fo.contact.mailtoHint}</p>
  </div>
</form>

<script define:vars={{ emailTo: site.email, requiredText: fo.contact.required }}>
  const form = document.querySelector("[data-contact-form]");
  const error = document.querySelector("[data-form-error]");
  if (form instanceof HTMLFormElement) {
    const select = form.querySelector("select[name=product]");
    const requested = new URLSearchParams(window.location.search).get("t");
    if (requested && select instanceof HTMLSelectElement) {
      const option = select.querySelector(`[data-id="${requested}"]`);
      if (option instanceof HTMLOptionElement) option.selected = true;
    }
    form.addEventListener("submit", (event) => {
      event.preventDefault();
      const data = new FormData(form);
      const name = String(data.get("name") ?? "").trim();
      const company = String(data.get("company") ?? "").trim();
      const phone = String(data.get("phone") ?? "").trim();
      const email = String(data.get("email") ?? "").trim();
      const product = String(data.get("product") ?? "").trim();
      const message = String(data.get("message") ?? "").trim();

      if (!name || !email || !message) {
        if (error instanceof HTMLElement) {
          error.textContent = requiredText;
          error.classList.remove("hidden");
        }
        return;
      }

      const subject = product ? `Betal: ${product}` : "Betal: samband";
      const body = [
        `Navn: ${name}`,
        company && `Fyritøka: ${company}`,
        phone && `Telefon: ${phone}`,
        `Teldupostur: ${email}`,
        product && `Tænasta: ${product}`,
        "",
        message,
      ]
        .filter((line) => line !== false)
        .join("\n");

      const url = `mailto:${emailTo}?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;
      window.location.href = url;
    });
  }
</script>
