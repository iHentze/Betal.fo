---
import { productIcons, type ProductIconId } from "../content/brand";

interface Props {
  id: string;
}

const { id } = Astro.props;
const asset = productIcons[(id as ProductIconId)] ?? productIcons.lon;
---

<img src={asset.src} alt={asset.alt} class="mx-auto h-48 w-auto md:h-56" />
