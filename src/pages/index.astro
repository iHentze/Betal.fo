---
import BaseLayout from "../layouts/BaseLayout.astro";
import ProductBento from "../components/ProductBento.astro";
import ValueGraphic from "../components/ValueGraphic.astro";
import MethodsRow from "../components/MethodsRow.astro";
import ClientMarquee from "../components/ClientMarquee.astro";
import HeroShowcase from "../components/HeroShowcase.astro";
import AnimatedCheckout from "../components/AnimatedCheckout.astro";
import BackofficeSimulator from "../components/BackofficeSimulator.astro";
import Wordmark from "../components/Wordmark.astro";
import Faq from "../components/Faq.astro";
import ClosingCta from "../components/ClosingCta.astro";
import { fo, products } from "../content/fo";
---

<BaseLayout>
  <!-- ============================= HERO ============================= -->
  <section class="grain relative isolate overflow-hidden">
    <div class="aurora -left-32 -top-40 h-[30rem] w-[30rem] bg-green/18"></div>
    <div class="aurora -right-40 top-24 h-[34rem] w-[34rem] bg-[#4b1fa8]/30"></div>

    <div class="pointer-events-none absolute inset-y-0 right-0 hidden w-[44%] lg:block">
      <img
        src="/brand/photo-tote.jpg"
        alt=""
        aria-hidden="true"
        class="h-full w-full object-cover object-[62%_center] opacity-30"
      />
      <div class="absolute inset-0 bg-gradient-to-r from-navy via-navy/80 to-navy/40"></div>
      <div class="absolute inset-0 bg-gradient-to-t from-navy via-transparent to-navy/70"></div>
    </div>

    <div class="relative mx-auto max-w-6xl px-5 pb-14 pt-14 sm:px-8 sm:pb-20 sm:pt-20">
      <!-- A definite second track: an auto track would size itself to the
           checkout's content, so the window width would shift between states. -->
      {/* The showcase's floating chip sits above its container (bottom-full),
          overhanging it by ~62px. At gap-16 that left a 2px visual gap on
          mobile, so the stacked layout needs a row gap that accounts for it. */}
      <div class="grid items-center gap-x-16 gap-y-32 lg:gap-16 lg:grid-cols-[1fr_23rem]">
        <div data-reveal-group="90">
          <div data-reveal>
            <Wordmark
              class="h-16 w-auto text-green drop-shadow-[0_0_45px_rgba(123,216,101,0.35)] sm:h-24"
              title="betal"
            />
          </div>

          <div
            class="mt-7 inline-flex w-fit items-center gap-2 rounded-full border border-green/25 bg-green/8 px-3.5 py-1.5"
            data-reveal
          >
            <span class="relative flex h-1.5 w-1.5">
              <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-green opacity-70"></span>
              <span class="relative inline-flex h-1.5 w-1.5 rounded-full bg-green"></span>
            </span>
            <span class="text-[11px] font-semibold uppercase tracking-[0.22em] text-green">{fo.home.eyebrow}</span>
          </div>

          <h1
            class="mt-5 text-[2.75rem] font-bold leading-[0.98] tracking-[-0.035em] text-white sm:text-7xl lg:text-[5rem]"
            data-reveal
          >
            {fo.home.title}
          </h1>

          <p class="mt-5 text-base font-semibold text-green sm:text-lg" data-reveal>
            {fo.home.kicker}
          </p>

          <p class="mt-4 max-w-xl text-lg leading-relaxed text-white/70" data-reveal>
            {fo.home.lead}
          </p>

          <div class="mt-9 flex flex-wrap items-center gap-3" data-reveal>
            <a
              href="#loysnir"
              class="group rounded-full bg-green px-7 py-3.5 text-sm font-semibold text-navy transition-all duration-300 active:scale-[0.98] hover:brightness-[1.08] hover:shadow-[0_0_35px_-6px_rgba(123,216,101,0.65)]"
            >
              <span class="flex items-center gap-2">
                {fo.home.primary}
                <span class="transition-transform duration-300 group-hover:translate-x-1" aria-hidden="true">→</span>
              </span>
            </a>
            <a
              href="/samband/"
              class="rounded-full border border-white/15 bg-white/[0.04] px-7 py-3.5 text-sm font-semibold text-white backdrop-blur-md transition-all duration-300 hover:border-white/30 hover:bg-white/[0.08]"
            >
              {fo.home.secondary}
            </a>
          </div>

          <ul class="mt-9 flex flex-wrap gap-x-6 gap-y-3 text-xs font-medium text-white/55" data-reveal>
            {
              fo.home.pills.map((item) => (
                <li class="flex items-center gap-2">
                  <svg class="h-3.5 w-3.5 shrink-0 text-green" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                    <path
                      fill-rule="evenodd"
                      d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z"
                      clip-rule="evenodd"
                    />
                  </svg>
                  {item}
                </li>
              ))
            }
          </ul>
        </div>

        <div class="flex justify-center lg:justify-end" data-reveal>
          <HeroShowcase />
        </div>
      </div>
    </div>
  </section>

  <!-- ====================== METHODS + REFERENCES ====================== -->
  <MethodsRow />
  <ClientMarquee />

  <!-- =========================== PRODUCTS =========================== -->
  <section id="loysnir" class="relative isolate scroll-mt-24 overflow-hidden">
    <div class="aurora -right-40 top-32 h-[30rem] w-[30rem] bg-[#4b1fa8]/25"></div>
    <div class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-24">
      <div class="mb-12 flex flex-col gap-6 sm:flex-row sm:items-end sm:justify-between" data-reveal-group="80">
        <div data-reveal>
          <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green">{fo.home.productsEyebrow}</p>
          <h2 class="mt-3 max-w-xl text-3xl font-bold leading-[1.05] tracking-[-0.03em] text-white sm:text-5xl">
            {fo.home.productsTitle}
          </h2>
        </div>
        <p class="max-w-sm text-sm leading-relaxed text-white/60" data-reveal>{fo.home.contrast}</p>
      </div>

      <ProductBento />

      <!-- Hard numbers under the solutions: what you get, and when -->
      {/* Same surface as the product cards above: gradient fill, hairline border
          and the cursor spotlight, rather than flat panels on the page colour.
          The divider is a real divide rule now, so the cells can be transparent
          and let the container's gradient show through. */}
      <dl
        class="spotlight mt-5 grid divide-y divide-white/10 overflow-hidden rounded-[1.75rem] border border-white/10 bg-gradient-to-b from-white/[0.05] to-white/[0.012] sm:grid-cols-2 sm:divide-x sm:divide-y-0"
        data-reveal
      >
        {
          fo.home.specs.map((spec) => (
            <div class="flex flex-col gap-1 px-7 py-6">
              <dt class="text-[11px] font-semibold uppercase tracking-[0.18em] text-white/40">{spec.label}</dt>
              <dd class="text-xl font-bold tracking-[-0.02em] text-white tabular-nums">{spec.value}</dd>
            </div>
          ))
        }
      </dl>
    </div>
  </section>

  <!-- ============================ VALUES ============================ -->
  <section class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-20">
    <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green" data-reveal>
      {fo.values.eyebrow}
    </p>
    <dl class="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3" data-reveal-group="110">
      {
        fo.values.items.map((item) => (
          <div
            class="spotlight group relative overflow-hidden rounded-3xl border border-white/10 bg-gradient-to-b from-white/[0.05] to-white/[0.012] p-7 transition-all duration-500 hover:-translate-y-1 hover:border-green/40"
            data-reveal
          >
            <span class="pointer-events-none absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-green/70 to-transparent opacity-0 transition-opacity duration-500 group-hover:opacity-100" />
            <ValueGraphic name={item.icon} />
            <dt class="text-xl font-bold tracking-[-0.02em] text-white transition-colors duration-300 group-hover:text-green">
              {item.title}
            </dt>
            <dd class="mt-3 text-sm leading-relaxed text-white/60">{item.text}</dd>
          </div>
        ))
      }
    </dl>
  </section>

  <!-- ========================= ONLINE / EPAY ========================= -->
  <section class="relative isolate overflow-hidden">
    <div class="aurora -left-40 top-10 h-[26rem] w-[26rem] bg-green/10"></div>
    <div class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-24">
      <div class="grid items-center gap-14 lg:grid-cols-2">
        <div data-reveal-group="80">
          <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green" data-reveal>
            {products[0].eyebrow}
          </p>
          <h2 class="mt-3 text-3xl font-bold leading-[1.05] tracking-[-0.03em] text-white sm:text-5xl" data-reveal>
            {fo.home.onlineTitle}
          </h2>
          <p class="mt-5 max-w-lg text-base leading-relaxed text-white/70" data-reveal>
            {fo.home.onlineLead}
          </p>

          <ul class="mt-8 space-y-3" data-reveal-group="70">
            {
              fo.home.onlinePoints.map((row) => (
                <li
                  class="spotlight flex items-start gap-3.5 rounded-2xl border border-white/8 bg-white/[0.025] p-4"
                  data-reveal
                >
                  <span class="mt-0.5 grid h-5 w-5 shrink-0 place-items-center rounded-full bg-green/15 text-[10px] font-bold text-green">
                    ✓
                  </span>
                  <span>
                    <span class="block text-sm font-semibold text-white">{row.title}</span>
                    <span class="mt-0.5 block text-xs leading-relaxed text-white/55">{row.text}</span>
                  </span>
                </li>
              ))
            }
          </ul>

          <a
            href={products[0].href}
            class="group mt-8 inline-flex items-center gap-2 text-sm font-semibold text-green"
            data-reveal
          >
            {fo.home.moreAbout} {products[0].eyebrow}
            <span class="transition-transform duration-300 group-hover:translate-x-1" aria-hidden="true">→</span>
          </a>
        </div>

        <div class="flex justify-center lg:justify-end" data-reveal>
          <AnimatedCheckout amount="1.249,00" />
        </div>
      </div>
    </div>
  </section>

  <!-- ========================= BRAND RIBBON ========================= -->
  <section class="relative overflow-hidden bg-green">
    <div class="pointer-events-none absolute -right-16 -top-20 h-64 w-64 rounded-full bg-white/25 blur-3xl"></div>
    <div
      class="relative mx-auto flex max-w-6xl flex-col gap-8 px-5 py-14 sm:px-8 sm:py-16 md:flex-row md:items-center md:justify-between"
    >
      <div>
        <Wordmark class="h-12 w-auto text-navy sm:h-16" title="betal" />
        <p class="mt-3 text-[11px] font-semibold uppercase tracking-[0.24em] text-navy/70">
          {fo.home.ribbonLabel}
        </p>
      </div>
      <div class="md:text-right">
        <p class="text-2xl font-bold tracking-tight text-navy sm:text-3xl">{fo.home.ribbonTitle}</p>
        <p class="mt-1 text-sm font-medium text-navy/70">{fo.home.ribbonText}</p>
      </div>
    </div>
  </section>

  <!-- ========================== BACKOFFICE ========================== -->
  <section class="mx-auto max-w-6xl px-5 py-16 sm:px-8 sm:py-24">
    <div
      class="spotlight overflow-hidden rounded-[2rem] border border-white/10 bg-gradient-to-br from-white/[0.05] to-transparent p-7 sm:p-12"
    >
      <div class="grid items-center gap-12 lg:grid-cols-2">
        <div data-reveal-group="80">
          <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green" data-reveal>
            {fo.home.backofficeEyebrow}
          </p>
          <h2 class="mt-3 text-3xl font-bold leading-[1.05] tracking-[-0.03em] text-white sm:text-4xl" data-reveal>
            {fo.home.backofficeTitle}
          </h2>
          <p class="mt-4 max-w-lg text-sm leading-relaxed text-white/65 sm:text-base" data-reveal>
            {fo.home.backofficeLead}
          </p>
          <ul class="mt-6 flex flex-wrap gap-2" data-reveal>
            {
              fo.home.backofficeTags.map((tag) => (
                <li class="rounded-full border border-white/10 bg-white/[0.04] px-3 py-1.5 text-xs text-white/70">
                  {tag}
                </li>
              ))
            }
          </ul>
          <a
            href={products[2].href}
            class="group mt-8 inline-flex items-center gap-2 text-sm font-semibold text-green"
            data-reveal
          >
            {fo.home.backofficeCta}
            <span class="transition-transform duration-300 group-hover:translate-x-1" aria-hidden="true">→</span>
          </a>
        </div>
        <div class="flex justify-center" data-reveal>
          <BackofficeSimulator />
        </div>
      </div>
    </div>
  </section>

  <!-- ============================ SOFTPAY ============================ -->
  <section class="mx-auto max-w-6xl px-5 pb-16 sm:px-8 sm:pb-24">
    <div
      class="grid items-center overflow-hidden rounded-[2rem] border border-white/10 bg-gradient-to-b from-white/[0.045] to-transparent md:grid-cols-2"
      data-reveal
    >
      <div class="order-2 p-7 sm:p-12 md:order-1">
        <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green">{products[1].eyebrow}</p>
        <h2 class="mt-3 text-3xl font-bold leading-[1.05] tracking-[-0.03em] text-white sm:text-4xl">
          {products[1].title}
        </h2>
        <p class="mt-4 max-w-lg text-sm leading-relaxed text-white/65 sm:text-base">
          {fo.home.softpayLead}
        </p>
        <a
          href={products[1].href}
          class="group mt-8 inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/[0.05] px-6 py-3.5 text-sm font-semibold text-white transition-all duration-300 hover:border-green/40 hover:bg-white/[0.09]"
        >
          {fo.home.softpayCta}
          <span class="transition-transform duration-300 group-hover:translate-x-1" aria-hidden="true">→</span>
        </a>
      </div>
      <div class="relative order-1 min-h-[19rem] w-full overflow-hidden md:order-2">
        <img src="/brand/photo-app-icon.jpg" alt="Betal-appin á telefonini" class="h-full w-full object-cover" />
        <div class="absolute inset-0 bg-gradient-to-r from-navy/70 via-transparent to-transparent md:from-navy/80"></div>
      </div>
    </div>
  </section>

  <!-- ============================= STEPS ============================= -->
  {/* The section pins for three screens and the steps change under it while the
      bar fills continuously. Progressive enhancement, not decoration: the
      markup below is a plain readable list, and the script switches on the pin
      only when it can actually run it. With no JS, or under reduced motion, you
      get the three steps stacked and the bar full.

      Two things this depends on, both easy to break again:
      - No overflow-hidden on the band. It would make the band a scroll
        container, and a sticky descendant can never stick past the edge of one.
        The glow is clipped by its own layer instead.
      - No data-reveal on the section or the track. The reveal system animates
        `transform`, and a transformed ancestor turns a sticky descendant into a
        static one. */}
  <section class="grain relative isolate mb-16 border-y border-white/[0.07] bg-navy-deep sm:mb-24" data-pin>
    <div class="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true">
      <div class="aurora -bottom-48 -left-32 h-[30rem] w-[34rem] bg-green/10"></div>
    </div>

    <div class="pin-track relative" data-pin-track>
      <div class="pin-stage">
        <div class="mx-auto w-full max-w-6xl px-5 py-16 sm:px-8 sm:py-24">
          <div class="max-w-3xl">
            <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green">
              {fo.steps.eyebrow}
            </p>
            <h2 class="mt-3 text-balance text-3xl font-bold leading-[1.05] tracking-[-0.03em] text-white sm:text-5xl">
              {fo.steps.title}
            </h2>
          </div>

          {/* Where you are, discretely. The bar says the same thing
              continuously; this says it in threes. */}
          <div class="pin-dots mt-8 flex gap-2 sm:mt-10" aria-hidden="true" data-pin-dots>
            {fo.steps.items.map(() => <span />)}
          </div>

          {/* role="list" because Preflight strips list-style and VoiceOver drops
              list semantics with it. Every step stays in the document and stays
              readable to a screen reader — only the painting is one at a time —
              so the list is announced in full regardless of scroll position. */}
          <ol role="list" class="pin-panels mt-8 sm:mt-10" data-pin-panels>
            {
              fo.steps.items.map((text, index) => (
                <li class="pin-panel">
                  <span
                    aria-hidden="true"
                    class="block text-[2.75rem] font-light leading-none text-white/90 sm:text-[4rem] lg:text-[5rem]"
                  >
                    {index + 1}
                  </span>
                  <p class="mt-4 max-w-[42rem] text-base leading-[1.75] text-white/75 [overflow-wrap:break-word] sm:text-[1.0625rem] lg:text-[1.125rem]">
                    {text}
                  </p>
                </li>
              ))
            }
          </ol>

          {/* Starts full so that a reader without JS sees a finished bar rather
              than an empty one. The script sets it every frame from there. */}
          <div class="step-rule mt-12 sm:mt-16" style="--tick: 100%" aria-hidden="true" data-pin-bar></div>
        </div>
      </div>
    </div>
  </section>

  <!-- ============================= ABOUT ============================= -->
  <section class="mx-auto max-w-6xl px-5 pb-16 sm:px-8 sm:pb-24">
    <div
      class="grid items-center overflow-hidden rounded-[2rem] border border-white/10 bg-gradient-to-b from-white/[0.045] to-transparent md:grid-cols-2"
      data-reveal
    >
      <div class="relative min-h-[19rem] w-full overflow-hidden">
        <img src="/brand/photo-card.jpg" alt="Betal-kort við skriftmerki" class="h-full w-full object-cover" />
        <div class="absolute inset-0 bg-gradient-to-l from-navy/70 via-transparent to-transparent"></div>
      </div>
      <div class="p-7 sm:p-12">
        <p class="text-[11px] font-semibold uppercase tracking-[0.24em] text-green">{fo.home.aboutEyebrow}</p>
        <h2 class="mt-3 text-3xl font-bold leading-[1.05] tracking-[-0.03em] text-white sm:text-4xl">
          {fo.home.aboutTitle}
        </h2>
        <p class="mt-4 max-w-lg text-sm leading-relaxed text-white/65 sm:text-base">{fo.home.about}</p>
        <a
          href="/samband/"
          class="mt-8 inline-flex rounded-full bg-green px-7 py-3.5 text-sm font-semibold text-navy transition-all duration-300 active:scale-[0.98] hover:brightness-[1.08] hover:shadow-[0_0_30px_-6px_rgba(123,216,101,0.6)]"
        >
          {fo.home.secondary}
        </a>
      </div>
    </div>
  </section>

  <!-- ============================== FAQ ============================== -->
  <section class="mx-auto max-w-6xl px-5 pb-24 sm:px-8">
    <Faq items={fo.faq.items} />
  </section>

  <ClosingCta />
</BaseLayout>

<style>
  /*
   * The bar under the steps. Green from the left edge out to --tick, track
   * after it, and a lit head sitting on the boundary. Registering --tick as a
   * real percentage makes it animatable, but while the section is pinned the
   * script writes it every frame, so the value is driven rather than eased —
   * the bar tracks the scroll exactly instead of chasing it.
   */
  @property --tick {
    syntax: "<percentage>";
    inherits: true;
    initial-value: 0%;
  }

  .step-rule {
    position: relative;
    height: 2px;
    width: 100%;
    background-image: linear-gradient(
      to right,
      var(--color-green) var(--tick, 0%),
      rgba(255, 255, 255, 0.09) var(--tick, 0%)
    );
  }

  /* The head. `left` is driven by --tick, so it rides the fill exactly. The
     ring is navy-deep, the band's ground, so the head punches out of the bar
     rather than merging with it. */
  .step-rule::after {
    content: "";
    position: absolute;
    left: var(--tick, 0%);
    top: 50%;
    height: 9px;
    width: 9px;
    margin-top: -4.5px;
    margin-left: -4.5px;
    border-radius: 9999px;
    background: var(--color-green);
    box-shadow:
      0 0 0 3px var(--color-navy-deep),
      0 0 18px rgba(123, 216, 101, 0.55);
  }

  .pin-dots span {
    display: block;
    height: 3px;
    width: 1.75rem;
    border-radius: 9999px;
    background: rgba(255, 255, 255, 0.16);
    transition: background 300ms var(--ease-out-expo);
  }

  .pin-dots span.is-on {
    background: var(--color-green);
  }

  /*
   * Unpinned is the authored state: a plain stacked list that reads correctly
   * with no script at all. Everything below only applies once the script has
   * confirmed it can run the pin.
   */
  .pin-panel + .pin-panel {
    margin-top: 2.5rem;
  }

  [data-pin].is-pinned .pin-track {
    height: 260vh;
  }

  [data-pin].is-pinned .pin-stage {
    position: sticky;
    /* Clears the sticky header — 4rem on phones, 4.5rem from sm up. Anything
       that sticks at 0 slides underneath it. */
    top: 4rem;
    min-height: calc(100vh - 4rem);
    min-height: calc(100svh - 4rem);
    display: flex;
    align-items: center;
  }

  /* Panels share one grid cell, so the row is as tall as the longest step and
     the bar below never jumps as they swap. */
  [data-pin].is-pinned .pin-panels {
    display: grid;
  }

  [data-pin].is-pinned .pin-panel {
    grid-area: 1 / 1;
    margin-top: 0;
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 420ms var(--ease-out-expo),
      transform 420ms var(--ease-out-expo);
  }

  [data-pin].is-pinned .pin-panel.is-on {
    opacity: 1;
    transform: none;
  }

  @media (min-width: 40rem) {
    [data-pin].is-pinned .pin-track {
      height: 300vh;
    }

    [data-pin].is-pinned .pin-stage {
      top: 4.5rem;
      min-height: calc(100vh - 4.5rem);
      min-height: calc(100svh - 4.5rem);
    }
  }

  /* The script never pins under this preference, so .is-pinned is absent and
     the stacked list stands. This only neutralises the dot easing. */
  @media (prefers-reduced-motion: reduce) {
    .pin-dots span {
      transition: none;
    }
  }
</style>

<script>
  /*
   * Maps scroll position through the tall track onto the three steps.
   *
   * Runs on every client-side navigation, not just the first load: ClientRouter
   * replaces the DOM, so these are new elements each time and the window
   * listener from the previous page has to come off.
   */
  function initPin() {
    const section = document.querySelector<HTMLElement>("[data-pin]");
    if (!section) return;

    const track = section.querySelector<HTMLElement>("[data-pin-track]");
    const bar = section.querySelector<HTMLElement>("[data-pin-bar]");
    const panels = Array.from(section.querySelectorAll<HTMLElement>("[data-pin-panels] > li"));
    const dots = Array.from(section.querySelectorAll<HTMLElement>("[data-pin-dots] > span"));
    if (!track || !bar || panels.length === 0) return;

    // Taking three screens of someone's scroll is a motion effect. Under this
    // preference the authored stacked list is the right answer, so leave it.
    if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
      dots.forEach((dot) => dot.classList.add("is-on"));
      return;
    }

    section.classList.add("is-pinned");

    let queued = false;

    const frame = () => {
      queued = false;

      const rect = track.getBoundingClientRect();
      // How far the track can travel while the stage is stuck to the top.
      const travel = rect.height - window.innerHeight;
      const progress = travel > 0 ? Math.min(1, Math.max(0, -rect.top / travel)) : 0;

      // floor() would put the last step on screen for a single pixel at exactly
      // 1, so the final index is clamped instead.
      const active = Math.min(panels.length - 1, Math.floor(progress * panels.length));

      panels.forEach((panel, i) => panel.classList.toggle("is-on", i === active));
      dots.forEach((dot, i) => dot.classList.toggle("is-on", i <= active));
      bar.style.setProperty("--tick", `${(progress * 100).toFixed(2)}%`);
    };

    const onScroll = () => {
      if (queued) return;
      queued = true;
      requestAnimationFrame(frame);
    };

    frame();
    window.addEventListener("scroll", onScroll, { passive: true });
    window.addEventListener("resize", onScroll, { passive: true });

    // The listeners are on window, which survives the swap, so they have to be
    // removed before the next page adds its own.
    document.addEventListener(
      "astro:before-swap",
      () => {
        window.removeEventListener("scroll", onScroll);
        window.removeEventListener("resize", onScroll);
      },
      { once: true },
    );
  }

  document.addEventListener("astro:page-load", initPin);
</script>
